http metodo de comunicação cliente/servidor, com requests e responses

	metodos:
		get:
		post:
	headers: todo tipo de informação pro servidor indentificar origem e outras coisas


Manipulação de formularios php:$_GET e $_POST

get = dados no header da request
post = dados no corpo da request


$_POST = variavel que recupera dados enviados por metodo post no lado do servidor


empty()
strlen()

is_integer()
is_numeric()
is_string()
